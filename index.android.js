/**
 * Sample React Native App
 * https://github.com/facebook/react-native
 * @flow
 */

import React, { Component } from 'react';
import {
  AppRegistry,
  StyleSheet,
  Text,
  View
} from 'react-native';

export default class olz extends Component {
  render() {
    return (
      <View>
        {/* TODO... Please check index.ios.js instead */}
      </View>
    );
  }
}

AppRegistry.registerComponent('olz', () => olz);
